import React from 'react';
import '../css/Main.css';

const HeroSection = () => (
  <section className="hero-wrapper">
    <h1 className="hero-title">Welcome to Movie Trailer Rendy</h1>
    <button className="hero-button">Explore Now</button>
  </section>
);

export default HeroSection;
